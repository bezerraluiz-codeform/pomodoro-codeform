# Fonte de verdade das envs: `pomodoro-codeform-backend/plataforma-infra/docker-compose.yml`
#
# Copie este arquivo para `.env` apenas se você NÃO estiver rodando via docker-compose.

API_URL=http://localhost:3333
ENV_NAME=local
DEBUG=true
